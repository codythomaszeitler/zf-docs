import {describe, it} from '@jest/globals';

describe('<ZeitlerForceNavigation>', () => {
    it('should do the thing', () => {
        console.log('This should be a print statement within navigation.test.tsx.');
    });

    function renderTestObject() {
    }
});